[package]
name = "esprobe"
description = "Use an ESP32-C3 as an SWD debug probe, over USB or Wi-Fi"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
name = "esprobe"
path = "src/lib.rs"

[[bin]]
name = "esprobe"
path = "src/main.rs"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
ctrlc = "3.4"
esprobe-protocol = { path = "../esprobe-protocol" }
probe-rs = "0.31"
serialport = { version = "4.8", default-features = false }
rpassword = "7.3"
sha2 = "0.10"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
gdbstub = "0.7"
gdbstub_arch = "0.3"
